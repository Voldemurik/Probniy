Удаление:

/* =====================================================
               ОТВЕТЫ БОТА
               ===================================================== */

            const botReplies = [

                'Привет! Как дела?',

                'Отлично, а у тебя?',

                'Расскажи подробнее.',

                'Я тебя понимаю.',

                'Здорово! 😊',

                'Может, попьём чаю? ☕',

                'Спасибо за сообщение!',

                'Это интересно...',

                'Хорошего дня!',

                'Понял, принято.'

            ];




                /* Ответ бота */

                setTimeout(() => {

                    const reply =
                        botReplies[
                            Math.floor(
                                Math.random() *
                                botReplies.length
                            )
                        ];


                    const replyTime =
                        new Date().toLocaleTimeString(
                            [],
                            {
                                hour: '2-digit',
                                minute: '2-digit'
                            }
                        );


                    renderMessage(
                        reply,
                        'bot',
                        replyTime,
                        true
                    );

                }, 400 + Math.random() * 600);


Добавляем:


            /* =====================================================
               КНОПКА "ОТПРАВИТЬ"
               ===================================================== */

            sendButton.addEventListener(
                'click',
                () => {

                    /*
                     * Ничего не делаем, если файл не выбран
                     * или этот файл уже был отправлен.
                     */

                    if (
                        fileInput.files.length === 0 ||
                        sendButton.disabled
                    ) {

                        return;

                    }


                    const file =
                        fileInput.files[0];


                    /* Сообщение пользователя о загруженном файле. */

                    addUserMessage(
                        `Файл "${file.name}" загружен.`
                    );


                    /*
                     * Единственный автоматический ответ
                     * ассистента после отправки файла.
                     */

                    renderMessage(
                        'Выберете опцию из списка ниже',
                        'bot',
                        null,
                        true
                    );


                    /*
                     * Показываем быстрые предложения
                     * только после отправки файла.
                     */

                    suggestions.style.display =
                        'flex';


                    /*
                     * Блокируем повторную отправку
                     * этого же файла.
                     *
                     * При выборе нового файла кнопка
                     * снова станет активной.
                     */

                    sendButton.disabled =
                        true;

                }
            );  


